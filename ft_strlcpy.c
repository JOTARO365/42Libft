#include "libft.h"
#include <stdio.h>

size_t	strlcpy(char *dst, const char *src, size_t size)
{
	size_t		i;
	unsigned char	*d;
	unsigned char	*s;

	d = (unsigned char *)dst;
	s = (unsigned char *)src;
	i = 0;
	while (src[i])
	{
		i++;
	}
	if (i
}
